<Window x:Class="YalaPcAgent.CountdownOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="YALA" WindowStyle="None"
        AllowsTransparency="False" Background="#0B0F17"
        Topmost="True" ShowInTaskbar="False"
        ShowActivated="False" Focusable="False" IsTabStop="False"
        ResizeMode="NoResize" SizeToContent="Manual"
        Width="250" Height="96"
        WindowStartupLocation="Manual"
        Left="20" Top="20" Loaded="OnLoaded">
    <!-- ขนาดคงที่ + ไม่โปร่งใส: ไม่เป็น layered window และไม่มีการเปลี่ยนขนาด
         จึงไม่บังคับให้ DWM recomposite ทับเกม fullscreen (เกมไม่เด้งออก) -->
    <Grid Margin="14,8,14,8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="TimeText" Grid.Row="0" FontSize="26" FontWeight="Bold"
                   Foreground="White" FontFamily="Consolas"/>
        <TextBlock x:Name="CodeText" Grid.Row="1" FontSize="11" Foreground="#94A3B8"/>
        <TextBlock x:Name="WarnText" Grid.Row="2" FontSize="13" FontWeight="Bold"
                   Foreground="#F87171" TextWrapping="NoWrap" Text=""/>
    </Grid>
</Window>
